// vite.config.js
import { defineConfig } from 'vite'

export default defineConfig({
  base: '/8_ball_pool_HACK_TOOL/', // 👈 repo name exactly
  build: {
    outDir: 'docs' // 👈 put the build into docs/
  }
})
